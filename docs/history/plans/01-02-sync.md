# Rencana Implementasi: Sinkronisasi Ulang Milestone 1 & 2

Saya mendeteksi bahwa Anda telah memperbarui detail *checklist* pada file-file *milestone*. Meskipun secara keseluruhan *engine* dan UI telah berjalan, ada beberapa penyesuaian (*refactor*) yang perlu dilakukan agar proyek kita 100% sejajar dengan *rules* baru Anda sebelum kita melangkah penuh ke Tahap 3.

## User Review Required
> [!IMPORTANT]
> **Navbar & Footer Statis**: Di spesifikasi baru, Anda meminta "Navbar & Footer statis" dan "TechGrid.astro". Sebelumnya, saya membuatnya sebagai React (`.tsx`). Apakah Anda ingin saya mengubah `Navbar`, `Footer`, dan `TechGrid` sepenuhnya menjadi **Astro Components** (`.astro`) agar lebih ringan?

> [!TIP]
> **Font Tuku**: Saya berencana menggunakan *font* modern seperti **Outfit** atau **Inter** dari Google Fonts yang memiliki nuansa kasual tapi membumi. Apakah ada *font* spesifik yang Anda inginkan?

## Proposed Changes

---

### Penyesuaian Tahap 1 (Setup Engine)
#### [NEW] `.env`
- Membuat file `.env` di _root_ yang berisi kerangka `TURSO_DATABASE_URL` dan `TURSO_AUTH_TOKEN` agar skrip `check-env.ts` berjalan sempurna.

---

### Penyesuaian Tahap 2 (Frontend UI)
#### [MODIFY] `frontend/src/layouts/Layout.astro`
- Mengimpor *Google Fonts* (akan diatur ke *font-family* utama di `global.css`).

#### [DELETE] `TechGrid.tsx` -> [NEW] `TechGrid.astro`
- Mengonversi komponen `TechGrid` dari React menjadi komponen murni Astro sesuai spesifikasi baru.
- (Opsional) Mengonversi `Navbar` dan `Footer` menjadi `.astro` jika disetujui.

#### [MODIFY] `frontend/src/components/ProjectSlider.tsx`
- Menginstal dependensi `swiper` (`bun add swiper` di *frontend*).
- Menulis ulang `ProjectSlider.tsx` menggunakan integrasi *Swiper.js* untuk pengalaman geser (carousel) yang lebih profesional dan *smooth* dibanding CSS snap statis.

#### [NEW] `frontend/src/components/ContactForm.tsx`
- Membuat komponen formulir kontak interaktif (Nama, Email, Pesan, tombol Submit) dengan validasi statis ringan sebelum nantinya dihubungkan ke API Eden di Tahap 3.

## Verification Plan

- Menjalankan `bun run dev:frontend` dan menguji integrasi Swiper.js di _Project Slider_.
- Mengecek tampilan _font_ baru dan memastikan UI tetap responsif.
- Memastikan `ContactForm.tsx` muncul dengan desain *earthy* yang konsisten di `index.astro`.
